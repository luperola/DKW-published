# Example env file (DO NOT commit real secrets)
# Server
PORT=3000
NODE_ENV=development

# Optional: where catalogs live at runtime (local path or mounted volume)
CATALOGS_DIR=./data

# If you later use cloud storage (S3, etc.), add here:
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
